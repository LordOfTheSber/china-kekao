spring:
  jpa:
    properties:
      hibernate.format_sql: true
  flyway:
    clean-disabled: true

logging:
  level:
    dev.kekao: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

management:
  endpoint:
    health:
      show-details: always
