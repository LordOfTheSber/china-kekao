import { PagePlaceholder } from "@/pages/PagePlaceholder";

export function StudyPage() {
  return (
    <PagePlaceholder
      title="Study session"
      description="Recognition + production cards, FSRS-driven queue."
      taskId="TASK-020"
    />
  );
}
