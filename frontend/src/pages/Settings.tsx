import { PagePlaceholder } from "@/pages/PagePlaceholder";

export function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Daily limits, retention target, production mode, drawing help level."
      taskId="TASK-029"
    />
  );
}
