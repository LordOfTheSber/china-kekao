import { PagePlaceholder } from "@/pages/PagePlaceholder";

export function DecksPage() {
  return (
    <PagePlaceholder
      title="Decks"
      description="Browse system decks and subscribe to populate your study queue."
      taskId="TASK-028"
    />
  );
}
