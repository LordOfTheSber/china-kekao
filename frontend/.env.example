# Base URL for the backend API. When using the Vite dev server proxy,
# leave this as `/api` and configure VITE_API_PROXY instead.
VITE_API_BASE_URL=/api

# Upstream the dev server proxy forwards `/api` requests to.
VITE_API_PROXY=http://localhost:8080
